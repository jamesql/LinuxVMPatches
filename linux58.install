eol() {
  echo ">>> NOTE, 5.8.18 was the last maintenance release by Greg Kroah-Hartman."
  echo "    This kernel series is now marked EOL (end of life)."
  echo "    "
  echo "    It is recommend to move on to linux59 series."
  echo "    Use followed cmd to do that: 'sudo mhwd-kernel -i linux59'"
  echo "    "
}

post_upgrade() {
  eol
}

post_install() {
  eol
}
